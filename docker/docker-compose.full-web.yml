# 构建镜像：在容器内 Godot 导出 Web + nginx 对外提供
# 仓库根执行：docker compose -f docker/docker-compose.full-web.yml up --build

services:
  sudoku-web:
    build:
      context: ..
      dockerfile: docker/Dockerfile.web
    ports:
      - "8080:80"
